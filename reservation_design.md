# 予約システム（Apps Script）設計書

## 1. 概要（Purpose）

本ドキュメントは、Google カレンダーと Google Apps Script を利用して構築する **予約受付システム** の設計仕様をまとめたものである。  
ユーザーは Web アプリにアクセスし、**日付 → 時間枠 → 情報入力** のステップで予約を行う。  
予約完了時に Google カレンダーへイベントが自動登録される。

本システムは Google ログイン不要で利用でき、URL を共有することで一般ユーザーに公開可能とする。

---

## 2. 要件（Requirements）

### 2.1 機能要件

| ID   | 機能名                | 内容                                                                 |
| ---- | --------------------- | -------------------------------------------------------------------- |
| F-01 | 予約枠の可視化        | 土日祝のみを対象に、日付ごとに「◎ / △ / ×」などの空き状況を表示する  |
| F-02 | 日付選択 → 時間枠表示 | 日付一覧から日付をクリックした際、その日の空き時間枠一覧を表示する   |
| F-03 | 予約情報入力          | 名前、メールアドレス、利用規約への同意等を入力するフォームを提供する |
| F-04 | カレンダー登録        | 予約確定時に、指定の Google カレンダーへイベントを作成する           |
| F-05 | 二重予約防止          | 予約確定時に対象時間帯の既存イベントを確認し、二重予約を防止する     |
| F-06 | ログ保存（任意）      | 予約情報を Google スプレッドシートへ記録する                         |

### 2.2 非機能要件

| ID   | 要件         | 内容                                                                              |
| ---- | ------------ | --------------------------------------------------------------------------------- |
| N-01 | 可用性       | Web アプリ URL を共有するだけで利用可能であること                                 |
| N-02 | 認証         | 予約利用者は Google ログイン不要とする（匿名アクセス）                            |
| N-03 | セキュリティ | カレンダー操作は Apps Script オーナー権限で行い、利用者にカレンダー権限を与えない |
| N-04 | 運用性       | Google カレンダー上の予定編集により、予約の管理・変更が可能                       |
| N-05 | 拡張性       | 追加機能に対応可能な構成とする                                                    |

---

## 3. システム構成（Architecture）

mermaid
flowchart TD
U[ユーザー] --> WA[Apps Script Web アプリ]
WA --> GAS[Google Apps Script Backend]
GAS --> CAL[Google Calendar<br>（予約用カレンダー）]
GAS --> SHEET[Google Spreadsheet<br>（予約ログ）]

- フロントエンド：Apps Script の HTML Service による Web 画面
- バックエンド：Apps Script（Code.gs）
- 外部サービス：
  • Google Calendar（予約用カレンダー）
  • Google Spreadsheet（予約ログ用）

---

## 4. 画面フロー（UI Flow）

flowchart LR
A[1. 日付一覧画面<br>（土日祝のみ・◎/△/×）]
--> B[2. 選択日付の時間枠一覧<br>（空き枠のみ）]
--> C[3. 予約情報入力フォーム<br>（名前/メール/同意など）]
--> D[4. 予約完了画面]

## 5. 処理フロー（Backend Flow）

5.1 日付一覧ロード

1.  ユーザーが Web アプリ URL にアクセス（doGet 呼び出し）。
2.  フロントから google.script.run.getAvailability() を実行。
3.  getAvailability() にて以下を実施：
    • 対象期間（例：本日から 30 日先まで）の土日祝を列挙。
    • 各日付について、営業枠数と予約済み枠数を算出。
    • 空き状況を ◎ / △ / × 等に変換。 4. 日付とステータスの一覧を JSON でフロントへ返却。

5.2 時間枠ロード

1. ユーザーが日付をクリック。
2. フロントから選択日付を引数に google.script.run.getSlots(date) を実行。
3. getSlots(date) にて以下を実施：
   • 指定日付の営業時間内の候補枠を生成。
   • カレンダーから該当日の既存イベントを取得。
   • 予約済みと重複する枠を除外し、空き枠のみ返却。

5.3 予約確定処理

1. ユーザーがフォームに名前・メールアドレス・利用規約同意を入力し「予約」ボタン押下。
2. フロントから google.script.run.withSuccessHandler(...).reserve(payload) を実行。
3. reserve(payload) にて以下を実施：
   • 入力値検証（必須項目、形式チェック等）。
   • 対象時間帯に再度既存イベントがないか確認（最終二重予約チェック）。
   • 問題なければ CalendarApp.createEvent() によりイベント作成。
   • 任意でスプレッドシートに予約情報を 1 行追加。 4. 正常終了時は「予約完了」レスポンスを返却。
   エラー時はエラーメッセージを返却し、フロント側で表示。

## 6. データ構造（Data Structure）

### 6.1 Availability（空き状況）

`json
[
  {
    "date": "2025-11-15",
    "label": "2025-11-15(土)",
    "status": "◎"
  }
]
`

### 6.2 Slots（時間枠）

`json
[
  {
    "start": "2025-11-15T09:00:00+09:00",
    "end": "2025-11-15T11:00:00+09:00"
  }
]
`

### 6.3 Reservation Payload

`json
{
  "name": "山田太郎",
  "email": "example@gmail.com",
  "agree": true,
  "slots": [
    { "start": "...", "end": "..." }
  ]
}
`

---

## 7. API インターフェース

| 名称                | 引数         | 返り値         | 説明                   |
| ------------------- | ------------ | -------------- | ---------------------- |
| `getAvailability()` | なし         | 日付状況リスト | 土日祝の空き状況を返す |
| `getSlots(date)`    | 文字列 date  | 空き枠リスト   | 指定日の空き枠を返す   |
| `reserve(payload)`  | JSON payload | `{ok: true}`   | イベント作成           |

---

## 8. エラーハンドリング

### 8.1 サーバー側（Apps Script）

- カレンダー ID 不正
- 既存予定とのバッティング
- 入力不足
- 日付パースエラー
- ログ出力失敗

### 8.2 クライアント側（HTML）

- failureHandler による alert 表示
- 必須項目（名前/メール/同意）のチェック

---

## 9. デプロイ手順

1. Apps Script プロジェクト作成
2. Code.gs / index.html を貼り付け
3. getAvailability() を実行して権限付与
4. **デプロイ → 新しいデプロイ → Web アプリ**
5.

- 実行ユーザー：**自分**
- アクセス権：**全員（匿名ユーザーを含む）**

6. URL 発行

---

## 10. 運用フロー

### 10.1 カレンダー管理

- 予約は Google カレンダーに自動登録
- キャンセルはイベント削除で対応
- 枠の変更は SLOT_DEFS の編集

### 10.2 ログ管理

- Spreadsheet の log シートに自動記録
- 過去の予約履歴確認や集計に利用可

---

## 11. セキュリティ

- カレンダー操作は Apps Script オーナー権限
- 利用者はカレンダー権限不要
- URL を知っている人のみ利用可能
- reCAPTCHA の追加も可能

---

## 12. 拡張案（Optional）

- 予約完了メール通知
- 予約キャンセルフォーム
- メニューごとの枠時間切替
- 管理者画面
- 祝日 API 連携
- 二段階メール認証

---

## 13. ファイル構成（Files）

`text
/reservation_form/
├── Code.gs
├── index.html
└── reservation_design.md
`
